{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@now/node-server",
      "config": { "zeroConfig": true }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "index.js" }]
}
